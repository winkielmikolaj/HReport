@{
ViewData["Title"] = "Complaints";
}

@model List<InfoReport>

<div class="container">
    <div class="row pt-4">
        <div class="col-6">
            <a class="btn btn-primary" asp-controller="EditAdd" asp-action="AddComplaint">
                Create new Complaint
            </a>
        </div>
        <div class="col-6 text-end">
            <h2>Complaints to HR</h2>
        </div>
    </div>
</div>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
        </thead>
        <tr>
            <th>Complaint</th>
            <th>Description</th>
            <th>Date</th>
            <th>IsChecked</th>
            <th>Actions</th>
        </tr>
        <tbody>
        @foreach (var entry in Model)
        {
        <tr class="@(entry.IsChecked ? "text-decoration-line-through" : "")">
            <td>@entry.Complaint</td>
            <td>@entry.Description</td>
            <td>@entry.Date</td>
            <td>
                @using (Html.BeginForm("ToggleCompletion", "EditAdd", new { id = @entry.Id }, FormMethod.Post))
                {
                <input type="checkbox"
                       name="IsCompleted"
                       onchange="this.form.submit();"
                       @(entry.IsChecked ? "checked" : "") />
                }
            </td>
            <td>
                <a asp-controller="EditAdd" asp-action="Edit" asp-route-id="@entry.Id" class="btn btn-primary btn-sm">
                    Edit
                </a>
                <a asp-controller="EditAdd" asp-action="Delete" asp-route-id="@entry.Id" class="btn btn-danger btn-sm">
                    Delete
                </a>
            </td>
        </tr>
        }
        </tbody>
    </table>
</div>
